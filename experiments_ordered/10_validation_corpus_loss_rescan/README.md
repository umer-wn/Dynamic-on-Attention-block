# 10_validation_corpus_loss_rescan — Validation corpus loss rescan

状态：`full_complete`

使用 The Pile validation/test 与 Paloma 代表性子集重新评估 Pythia early checkpoints 的 loss，并与既有 Frobenius/Jacobian 合并分析。

数据根：

```text
/home/luohaoming/model_feature_experiments/pythia_validation_corpus_loss_rescan
```
